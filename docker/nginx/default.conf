server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/javascript application/json;

    # SPA: cualquier ruta de Angular Router que no sea un archivo real cae a index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
